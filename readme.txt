If on mac: brew install portaudio
Activate Envelope:
source venv/Scripts/activate

Windows: Super confusing. ttsx3 only works with pipenv shell. 
Use this instead. 
pip install pipenv (Do this not in virtual environment) 
pipenv install pyttsx3 